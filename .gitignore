# environment
venv/
__pycache__/
*.pyc
.claude/
.vscode/

# model weights ARE tracked (checkpoints/, 27 MB each) so teammates can run the
# app from a clone. Only the raw Kaggle run download is excluded (duplicates them).
kaggle/run_*/

# personal config copy (see README -> "Point the project at your data")
config.local.yaml

# image data lives at C:/ml-data/nih-cxr, never in the repo.
# Exception: the small per-class exemplar thumbnails (NIH data is CC0).
outputs/predictions/briefing_samples/

# scratch
image.png
outputs/reports/run.log
outputs/reports/run.err
*.tmp
